package com.safecar.platform.workshop.domain.model.valueobjects;

/**
 * Service Order Status - Status for Workshop Service Orders.
 * @param OPEN   the service order is open
 * @param CLOSED the service order is closed
 */
public enum ServiceOrderStatus {
    OPEN,
    CLOSED
}